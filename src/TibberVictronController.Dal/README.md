# Data Access Layer

Owns database persistence and repository implementations.

This project may depend on `Business` for domain types and repository contracts, but `Business` must not depend on this project.
